# R9A0 Native ChatGPT Project Repair Packet V1

## Status

`IN_PROGRESS`

## Controlling authority

Patrick established that Voss leads the R9A0 program and that Voss and The Enforcer own the native ChatGPT Project files and Project Instructions.

- Coordinator, assignment owner, integration controller, and final verifier: `auditor/voss`
- Sole native-file implementation writer: `workstream/build-enforcer`
- Supabase construction writer: `debug/masamune/masa`
- Independent Supabase verifier: `debug/masamune/mune`

This packet is executable working-project authority within the exact repository lease below. It does not authorize merge, deployment, production mutation, credentials, force push, deletion, or ChatGPT Project installation.

## Repository lease

- Repository: `thebrazenbeard/vera-R9A0`
- Branch: `feature/r9a0-native-project-v1`
- Exact base SHA: `71b3fc4892df3a70e493e287542e68bfa1e5a798`
- Writer: `workstream/build-enforcer`
- Supabase coordination receipt: sequence `3135`
- Update rule: non-force commits only

Allowed paths:

- `project/**`
- `docs/native-project/**`
- `schemas/native-project/**`
- `scripts/validate_r9a0_project.py`
- `tests/native-project/**`
- `.github/workflows/r9a0-native-project.yml`

All R9A0 native files, instructions, manifests, validators, tests, packaging metadata, and installation artifacts must remain in this repository.

## Objective

Build the complete successor native ChatGPT Project architecture and operational Project Instructions. Native construction proceeds independently of final hosted Supabase acceptance where the work does not depend on the final database contract.

Mune returned `CHANGES_REQUESTED` on database head `71b3fc4892df3a70e493e287542e68bfa1e5a798`. Therefore that head is a provisional integration input, not an accepted final backend binding. Native behavioral, packaging, validation, authority, and installation work proceeds. Final schema and RPC bindings wait for a corrected immutable database head and Voss reconciliation.

## Post-install defect register

### `INSTALLATION_CURRENT_STATE_PRECEDENCE`

Immutable package generation metadata must not override a later completed installation receipt and cold-start readback. Current installation claims resolve from current-state receipts before generation-time fields.

### `VOSS_DELEGATED_AUTHORITY_INHERITANCE`

A Voss assignment issued under Patrick's delegated coordinator authority is executable authority. Routine assigned work must not be returned to Patrick merely because Voss issued or will review it. Escalation is limited to genuine conflicts, user-only effects, prohibited effects, unclear targets, missing leases, or integrity failures.

### `IMMEDIATE_CORRECTION_UPTAKE`

After a concrete executable correction, the next response applies it immediately. Acknowledgement, apology, or promise does not count as completion. Pending obligations survive short follow-ups until delivered, blocked, or superseded.

### `CROSS_SURFACE_SHARED_STATE_CHECKS`

When project status, authority, unfinished work, or current integration state is material, inspect the relevant Supabase, GitHub, Basic Memory Cloud, and Google Drive surfaces before asking Patrick to relay exposed information. A stored row proves existence, not target consumption.

### `LOGICAL_BINDING_VS_NATIVE_CONNECTION_SEPARATION`

Treat connector accessibility, logical project binding, native GitHub/Supabase integration, deployment automation, and synchronization behavior as separate evidence classes. None may be inferred from another.

### `SUPABASE_AND_REPOSITORY_BINDINGS`

- R9A0 repository: `thebrazenbeard/vera-R9A0`
- Temporary hosted build ground: BT2 project `agvhmutlrolbaijzlbqk`
- Allowed hosted schemas: names beginning with `r9a0_`
- Reserved scopes: `public`, `governance`, `bt2_native`
- Vera production project `klmbpaigzeguvnpccqzz`: prohibited target for R9A0 construction

### `EXACT_HEAD_AND_WRITER_LEASE_ENFORCEMENT`

One writer may modify a branch or pull request during a stage. Reviews bind one immutable head. Later commits make earlier reviews historical. Review authority does not include patch authority without a transferred lease.

### `NO_HIDDEN_CONSUMPTION_OR_OFFSCREEN_WORK`

Coordination delivery does not prove a runtime consumed or executed an assignment. Never claim hidden work, private waiting, target consumption, successful persistence, or completed future work without exposed evidence.

### `ATOMIC_SUCCESSOR_INSTALLATION`

The successor set must be manifest-defined, uniquely named, checksum-bound, and packaged atomically. File availability, Project upload, Project Settings replacement, cold-start readback, and verified installation remain separate states. A completed receipt records installation without rewriting immutable package history.

### `POST_INSTALL_AUDIT_REGRESSION_SUITE`

Automated tests must reproduce the observed failures above and prove corrected behavior through deterministic positive, negative, and hostile cases.

## Required outputs

1. Complete successor ChatGPT Project file set under `project/`.
2. Operational native Project Instructions payload within the platform character limit.
3. Manifest, checksums, bundle, state, validation, and supersession records.
4. Deterministic validator and automated tests.
5. Atomic release-package definition and delivery inventory.
6. Installation receipt template and cold-start readback protocol.
7. Post-install audit checklist and hostile regression suite.
8. Supabase integration contract bound to the accepted Masamune database head.
9. Exact-head handoff listing every changed path, test result, limitation, and unresolved finding.

## Acceptance criteria

- Native instructions are operational and within the platform character limit.
- Completed installation receipts outrank immutable generation-state fields for current-state claims.
- Voss assignments execute without renewed user confirmation unless a genuine escalation condition exists.
- Concrete executable corrections are applied in the next response.
- Shared-state checks cover all relevant exposed surfaces.
- Logical bindings and native platform connections remain distinct evidence classes.
- All R9A0 database references honor the temporary BT2 namespace boundary.
- Vera production remains untouched.
- The release is atomic, deterministic, checksum-bound, and cold-start testable.
- Zero unresolved HIGH or MEDIUM findings remain at the accepted exact head.

## Dependency boundary

Native files and local or CI tests may be implemented now. Hosted integration execution and final acceptance require:

1. a corrected immutable Masamune database head;
2. Mune's independent verdict;
3. Voss reconciliation;
4. final native exact-head verification.

This dependency does not authorize idling on work that does not require the final database verdict.
